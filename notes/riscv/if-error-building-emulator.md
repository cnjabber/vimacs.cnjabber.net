Clean rocket-chip project files
===============================

In .gitignore in rocket-chip, there are `target/`, `project/target`. If you have problem building rocket chip after an update, you need to remove them.

